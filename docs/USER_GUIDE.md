# VSCode AI Assistant - 用户使用指南

> 让 AI 成为你的编程搭档，提升 10 倍开发效率

## 📋 目录

- [快速开始](#快速开始)
- [基础功能](#基础功能)
- [MCP 工具系统](#mcp-工具系统)
- [Skill 技能系统](#skill-技能系统)
- [高级功能](#高级功能)
- [配置说明](#配置说明)
- [常见问题](#常见问题)

---

## 快速开始

### 安装

1. 在 VSCode 扩展市场搜索 "AI Assistant"
2. 点击安装
3. 重启 VSCode

### 配置 API Key

打开设置 (`Ctrl+,`)，搜索 "AI Assistant"：

```json
{
  "aiAssistant.provider": "openai",  // 或 "anthropic", "deepseek"
  "aiAssistant.apiKey": "sk-xxx",
  "aiAssistant.model": "gpt-4"
}
```

### 打开聊天面板

- 快捷键: `Ctrl+Shift+A` (Windows/Linux) 或 `Cmd+Shift+A` (Mac)
- 命令面板: `AI Assistant: 打开聊天`
- 侧边栏: 点击 AI 图标

---

## 基础功能

### 1. 智能对话

直接在输入框中输入问题，AI 会根据上下文给出回答：

```
你: 解释一下这个函数的作用
AI: 这个函数是用来...
```

### 2. 上下文感知

AI 自动感知当前环境：

- **当前文件**: 自动获取你正在编辑的文件
- **选中代码**: 自动获取你选中的代码片段
- **项目结构**: 了解项目的整体架构

### 3. @ 提及

使用 `@` 快速引用：

| 语法 | 说明 | 示例 |
|------|------|------|
| `@file:路径` | 引用文件 | `@file:src/index.ts` |
| `@selection` | 引用选中代码 | `解释 @selection` |
| `@project` | 引用项目结构 | `分析 @project 的架构` |
| `@error` | 引用当前错误 | `修复 @error` |

### 4. 快捷命令

在输入框中输入 `/` 开头的命令：

| 命令 | 说明 |
|------|------|
| `/help` | 显示帮助信息 |
| `/clear` | 清空当前对话 |
| `/new` | 新建对话 |
| `/history` | 查看历史对话 |
| `/model` | 切换模型 |
| `/compact` | 压缩对话上下文 |

---

## MCP 工具系统

MCP (Model Context Protocol) 是一套标准的工具调用协议，让 AI 能够执行实际操作。

### 查看可用工具

```
@mcp:list
```

或点击聊天面板右上角的 🔧 图标。

### 工具调用语法

```
@mcp:工具名 参数1=值1 参数2=值2
```

### 常用工具示例

#### 📁 文件操作

```bash
# 读取文件
@mcp:read_file filePath=src/index.ts

# 写入文件
@mcp:write_file filePath=output.txt content="Hello World"

# 搜索文件
@mcp:search_files pattern=**/*.ts

# 搜索代码
@mcp:search_code query=function maxResults=10
```

#### 🔧 代码操作

```bash
# 分析代码
@mcp:analyze_code file=src/utils.ts type=full

# 格式化代码
@mcp:format_code file=src/index.ts

# 生成文档
@mcp:generate_docs file=src/api.ts style=jsdoc

# 获取诊断信息
@mcp:diagnostics file=src/main.ts severity=error

# 自动修复
@mcp:auto_fix file=src/main.ts
```

#### 🔀 Git 操作

```bash
# 查看状态
@mcp:git_status

# 提交更改
@mcp:git_commit message="feat: add new feature" all=true

# 查看差异
@mcp:git_diff staged=true

# 查看日志
@mcp:git_log count=10

# 切换分支
@mcp:git_checkout branch=develop create=false
```

#### 🧪 测试与构建

```bash
# 运行测试
@mcp:run_test pattern=*.test.ts

# 构建项目
@mcp:build command="npm run build"

# 生成测试
@mcp:gentest file=src/utils.ts framework=vitest
```

#### 📊 图表生成

```bash
# 生成流程图
@mcp:diagram type=flowchart source=file

# 生成类图
@mcp:diagram type=class source=project

# 根据描述生成
@mcp:diagram type=sequence description="用户登录流程"
```

### 任务执行可视化

当执行 MCP 工具时，你会看到清晰的执行过程：

```
┌─────────────────────────────────────────────┐
│ 🔧 MCP: 分析代码                            │
│ ───────────────────────────────────────────  │
│ ✅ 参数验证     [100%] ────────── 0.1s      │
│ ✅ 权限检查     [100%] ────────── 0.05s     │
│ 🔄 执行工具     [ 60%] ━━━━━━━━━━ 分析中... │
│ ⏳ 处理结果     [  0%] ────────── 等待      │
│                                              │
│ 已用时: 2.3s | 预计剩余: 1.5s               │
│                        [暂停] [取消]         │
└─────────────────────────────────────────────┘
```

---

## Skill 技能系统

Skill 是比 MCP 工具更高级的自动化能力，可以执行复杂的多步骤任务。

### 内置技能

#### 1. 代码审查 (CodeReviewer)

自动审查代码质量，提供改进建议：

```
帮我review当前文件的代码
```

审查内容包括：
- 代码风格
- 潜在bug
- 性能问题
- 安全漏洞
- 最佳实践

#### 2. 测试架构师 (TestArchitect)

智能生成测试用例：

```
为这个函数生成完整的单元测试
```

支持：
- Jest
- Vitest
- Mocha
- Pytest

#### 3. 依赖守护者 (DependencyGuardian)

管理项目依赖：

```
检查项目依赖是否有安全问题
```

功能：
- 检测过期依赖
- 安全漏洞扫描
- 未使用依赖检测
- 升级建议

#### 4. 工具生成器 (ToolMaker)

根据描述自动生成 MCP 工具：

```
帮我创建一个工具，用于批量重命名文件
```

### 技能执行可视化

```
┌─────────────────────────────────────────────┐
│ 🎯 Skill: 代码审查                          │
│ ───────────────────────────────────────────  │
│ Phase 1: 收集上下文                         │
│   ├─ ✅ 读取当前文件                        │
│   ├─ ✅ 分析项目结构                        │
│   └─ ✅ 检测语言框架                        │
│                                              │
│ Phase 2: 代码分析                           │
│   ├─ ✅ 静态分析                            │
│   ├─ 🔄 复杂度检测 (进行中...)              │
│   └─ ⏳ 最佳实践检查                        │
│                                              │
│ Phase 3: 生成报告                           │
│   └─ ⏳ 等待中...                           │
└─────────────────────────────────────────────┘
```

---

## 高级功能

### 1. 会话管理

#### 查看历史对话

点击侧边栏的 📜 图标，或输入 `/history`。

#### 继续上次对话

```
/continue
```

或使用快捷键 `Ctrl+Shift+C`。

#### 导出对话

右键点击对话，选择"导出"。

### 2. 上下文压缩

当对话过长时，AI 会自动压缩历史上下文：

```
/compact
```

压缩后会保留：
- 最近 4 条消息
- 关键信息摘要
- 重要决策记录

### 3. 记忆系统

AI 会自动记住：

- **用户偏好**: 你喜欢的代码风格、命名习惯等
- **项目知识**: 项目结构、技术栈、API约定等
- **历史上下文**: 之前讨论过的问题和解决方案

查看记忆统计：

```
/memory stats
```

清除记忆：

```
/memory clear
```

### 4. 任务中断与恢复

如果任务执行过程中被中断（网络问题、主动取消等），可以恢复：

```
/resume
```

或点击通知栏中的"恢复任务"按钮。

### 5. 多模型切换

在对话中动态切换模型：

```
/model gpt-4
/model claude-3
/model deepseek-v3
```

或点击状态栏中的模型名称。

---

## 配置说明

### 基础配置

```json
{
  // API 配置
  "aiAssistant.provider": "openai",
  "aiAssistant.apiKey": "sk-xxx",
  "aiAssistant.model": "gpt-4",
  "aiAssistant.baseUrl": "",  // 自定义 API 端点
  
  // 行为配置
  "aiAssistant.autoContext": true,      // 自动获取上下文
  "aiAssistant.streamResponse": true,   // 流式响应
  "aiAssistant.language": "zh-CN",      // 界面语言
  
  // 上下文压缩
  "aiAssistant.contextCompression.enabled": true,
  "aiAssistant.contextCompression.maxMessages": 20,
  "aiAssistant.contextCompression.keepRecent": 4,
  
  // 会话历史
  "aiAssistant.sessionHistory.maxSessions": 50,
  "aiAssistant.sessionHistory.autoSave": true
}
```

### 快捷键配置

| 功能 | Windows/Linux | Mac |
|------|---------------|-----|
| 打开聊天 | `Ctrl+Shift+A` | `Cmd+Shift+A` |
| 发送消息 | `Ctrl+Enter` | `Cmd+Enter` |
| 新建对话 | `Ctrl+Shift+N` | `Cmd+Shift+N` |
| 继续对话 | `Ctrl+Shift+C` | `Cmd+Shift+C` |
| 取消生成 | `Escape` | `Escape` |

---

## 常见问题

### Q: API 调用失败？

1. 检查 API Key 是否正确
2. 检查网络连接
3. 确认 API 余额充足
4. 尝试切换 API 端点

### Q: 响应太慢？

1. 切换到更快的模型 (如 gpt-3.5-turbo)
2. 减少上下文长度
3. 使用 `/compact` 压缩历史

### Q: 工具执行失败？

1. 检查参数是否正确
2. 查看错误日志
3. 确认文件/目录权限
4. 尝试重新执行

### Q: 如何查看执行日志？

打开 VSCode 输出面板 (`Ctrl+Shift+U`)，选择 "AI Assistant"。

### Q: 如何重置插件？

```
/reset
```

或在命令面板中执行 "AI Assistant: 重置配置"。

---

## 反馈与支持

- 🐛 Bug 反馈: [GitHub Issues](https://github.com/xxx/vscode-ai-assistant/issues)
- 💡 功能建议: [GitHub Discussions](https://github.com/xxx/vscode-ai-assistant/discussions)
- 📖 完整文档: [在线文档](https://docs.ai-assistant.dev)

---

*Happy Coding with AI! 🚀*
